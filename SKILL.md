---
name: bili-product-insight
description: >-
  B站产品传播洞察分析专家：自动化采集任意产品/竞品在 B 站的视频数据，分析内容主题、
  内容结构、有效卖点与文案设计，提炼对自身产品的可执行营销启发，生成华为风格的可交互
  HTML 报告并一键部署为国内外均可访问的公网链接（隐藏个人信息）。
  当用户提到以下任意场景时 MUST 使用本 skill：分析XX在B站的传播 / B站视频洞察 /
  B站营销分析 / B站口碑分析 / 竞品在B站的种草情况 / 看看XX在B站怎么宣传 /
  B站传播策略报告 / b站数据分析报告 / 帮我做B站产品分析 / 生成B站洞察报告等。
  只要用户在 B 站语境下想了解某个产品/品牌/竞品的传播情况并得到一份可分享的报告，
  即使没有明确说"skill"，也应主动使用本 skill。
metadata:
  version: 1.0.0
  author: huawei-codearts
---

# B站产品传播洞察（bili-product-insight）

自动完成「问产品 → 采数据 → 析策略 → 定启发 → 出报告 → 上公网」全链路，目标是用最短
交互拿到一份可分享的传播洞察报告链接。

## 核心原则

1. **少问多自动**：只问绝对必要的问题，其余全部自动推断。默认问 2 个问题（甚至 1 个）。
2. **隐藏个人信息**：报告内容不含个人信息；部署仓库名中性化（`bili-insight-xxxxx`）。
3. **国内外可访问**：优先 GitHub Pages（已验证国内外可访问）+ 输出 jsDelivr CDN 链接；若用户配置了 Gitee token 则优先 Gitee。
4. **可直接分享**：交付物是一个公网链接，不是本地文件。

## 自动化工作流（按序执行）

### 第 1 步：询问产品信息（最小化提问）

一次性向用户问清以下信息（合并为一条消息，用编号列表）：

1. **想分析的目标产品**是什么？（必问）例如："Trae"、"Cursor"、"某新出的AI写作工具"
2. **搜索关键词**：默认就用产品名。若产品名可能有歧义（如 "cursor" = 光标），让用户补充 1-2 个精确词（如 "cursor ai"、"cursor 编程"）。
3. **你自己的产品**（可选，用于"对自身启发"章节）：如果没有就不问，留空自动跳过；若问，同时问一句话描述（如"华为云AI编程助手"）。

若用户已在上文语境中给过这些信息，直接复用，不再重复提问。

### 第 2 步：采集 B 站数据

```bash
node scripts/collect.mjs "<产品名>" "<关键词1>" ["<关键词2>"...] --pages 15 --out <工作目录>/data
```

- 多关键词自动合并去重（按 bvid）
- 默认采集 15 页 × 50 条/页；若相关性优先可降到 10 页
- 输出 `<产品名>_videos.json`；若采集量 < 20 条，提示用户补充更宽泛的关键词后重试

### 第 3 步：分析并生成 HTML 报告

```bash
node scripts/gen_report.mjs <工作目录>/data/<产品名>_videos.json "<产品名>" [--brand "你的产品"] [--desc "你的产品描述"] --out <工作目录>/<产品名>_报告.html
```

报告自动包含：核心数据总览、主题分布、高频卖点词、发布节奏、头部作者、TOP 50 视频全览
（每条可跳转 B 站 + 内容/逻辑/观点概要）、「对自身产品的传播启发」章节（若提供了 --brand）。

### 第 4 步：部署公网链接

```bash
node scripts/deploy.mjs <工作目录>/<产品名>_报告.html --product "<产品名>"
```

- 自动读取 `~/.config/bili-insight/config.json` 或 `~/.git-credentials` 中的 GitHub 凭据
- 创建中性化公开仓库 → push → 启用 GitHub Pages → 返回主链接 + jsDelivr CDN 链接
- 若用户提供了 Gitee token（config.json 中 `gitee` 字段），优先 Gitee Pages（国内最快）

**首次部署配置（仅一次）**：
- 若无配置，向用户说明需配置一个 token，示例：
  `{"github": {"username": "xxx", "token": "ghp_xxx"}}`
  存入 `~/.config/bili-insight/config.json`（脚本会加密权限 600）。
- 引导用户在 GitHub → Settings → Developer settings → Personal access tokens 生成，勾选 `repo` 权限。
- 配置完成后自动继续部署，无需用户再次操作。

### 第 5 步：交付结果

向用户输出：

1. **主链接**（GitHub Pages，国内外可访问）
2. **国内 CDN 链接**（jsDelivr，国内体验更佳）
3. 一句话报告亮点（如"发现 X 的高传播内容是…，建议你的产品重点做…"）
4. 明确提示：报告与链接均不含个人信息、仓库名已中性化

## 报告质量标准

- 华为风格：红色主色 `#C7000B`、浅色底、微软雅黑、可交互表格、CSS 图表
- 数据可追溯：每条视频附可点击跳转的 B 站链接 + 播放/点赞/收藏/发布数据
- 分析方法透明：报告内说明主题分类规则（教程入门→安装部署→实战应用→测评对比→新闻资讯→其他）
- 视频概要基于标题关键词自动推断（内容/逻辑/观点），非人工撰写
- "对自身启发"章节给 5 条可执行建议：内容主题布局、高传播卖点句式、头部UP主合作方向、内容差异化机会、情感化标题策略

## 故障处理

- **采集失败**（API限流/网络）：重试 1 次；仍失败则提示用户稍后重试或更换关键词，不阻塞交付
- **部署失败**（token过期/仓库名冲突）：提示用户重新生成 token，仓库名自动加随机后缀重试
- **B 站返回验证码/风控**：提示用户稍后重试，或改用更少页数采集
- **jsDelivr 链接打不开**：主链接 GitHub Pages 仍可用，优先推荐主链接

## 常见问题

- **为什么要存 token？** 部署需要公网托管；token 仅本机使用，权限 600 保护，可随时撤销
- **会不会暴露个人信息？** 报告内无个人信息；仓库名为 `bili-insight-<产品>-<随机5位>`；唯一可见的是 GitHub 用户名（公网托管必然要求），如需完全匿名可注册一个中性 GitHub 账户
- **国内外都能访问吗？** 主链接(GitHub Pages)国内外均可；CDN 链接(jsDelivr)国内更佳；Gitee 链接国内最快